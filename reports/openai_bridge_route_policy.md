# OpenAI Bridge Route Policy

- Generated: `2026-06-06T12:44:14Z`
- Local bridge policy complete: `True`
- Hosted OpenAI OAuth complete: `False`
- Bottom line: Local bridge policy covers every documented path, but strict hosted OpenAI OAuth is verified only for 5 paths; local compatibility is not hosted Platform OAuth.
- Official paths: `172`
- Direct hosted OAuth verified: `5`
- Local bridge compatibility: `167`
- Allow local bridge: `172`
- Deny local bridge: `0`
- Strict hosted OAuth allow: `5`
- Strict hosted OAuth deny: `167`
- Platform fallback candidates: `0`
- JSON: `reports/openai_bridge_route_policy.json`
- CSV: `reports/openai_bridge_route_policy.csv`

## Consumer Guidance

- Use allowlist_oauth_only only for paths with direct hosted OpenAI OAuth evidence.
- Use allowlist_local_bridge for local-proxy deployments; local substitutes are not hosted Platform OAuth proof.
- Block denylist_local_bridge unless the app explicitly enables Platform/Admin fallback or has live resource proof.
- Treat local_compat_or_chatgpt_backend_bridge as local compatibility, not hosted OpenAI OAuth support.
- Use reports/openai_bridge_ci_gate.sh path/to/app in CI to prevent accidental boundary routes.

## Policy Rows

| Local Path | Category | Policy | Mode | Credential Envs | Action |
|---|---|---|---|---|---|
| `/v1/assistants` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/assistants/{assistant_id}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/audio/speech` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/audio/transcriptions` | `direct_official_oauth_verified` | `allow` | `direct_official_oauth_or_local_proxy` | `` | Use the local proxy or the tested direct official OAuth path. Re-run the OAuth matrix for a different account or date. |
| `/v1/audio/translations` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/audio/voice_consents` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/audio/voice_consents/{consent_id}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/audio/voices` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/batches` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/batches/{batch_id}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/batches/{batch_id}/cancel` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/chat/completions` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/chat/completions/{completion_id}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/chat/completions/{completion_id}/messages` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/chatkit/sessions` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/chatkit/sessions/{session_id}/cancel` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/chatkit/threads` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/chatkit/threads/{thread_id}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/chatkit/threads/{thread_id}/items` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/completions` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/containers` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/containers/{container_id}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/containers/{container_id}/files` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/containers/{container_id}/files/{file_id}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/containers/{container_id}/files/{file_id}/content` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/conversations` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/conversations/{conversation_id}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/conversations/{conversation_id}/items` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/conversations/{conversation_id}/items/{item_id}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/embeddings` | `direct_official_oauth_verified` | `allow` | `direct_official_oauth_or_local_proxy` | `` | Use the local proxy or the tested direct official OAuth path. Re-run the OAuth matrix for a different account or date. |
| `/v1/evals` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/evals/{eval_id}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/evals/{eval_id}/runs` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/evals/{eval_id}/runs/{run_id}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/evals/{eval_id}/runs/{run_id}/output_items` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/evals/{eval_id}/runs/{run_id}/output_items/{output_item_id}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/files` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/files/{file_id}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/files/{file_id}/content` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/fine_tuning/alpha/graders/run` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/fine_tuning/alpha/graders/validate` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/fine_tuning/checkpoints/{fine_tuned_model_checkpoint}/permissions` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/fine_tuning/checkpoints/{fine_tuned_model_checkpoint}/permissions/{permission_id}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/fine_tuning/jobs` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/fine_tuning/jobs/{fine_tuning_job_id}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/fine_tuning/jobs/{fine_tuning_job_id}/cancel` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/fine_tuning/jobs/{fine_tuning_job_id}/checkpoints` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/fine_tuning/jobs/{fine_tuning_job_id}/events` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/fine_tuning/jobs/{fine_tuning_job_id}/pause` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/fine_tuning/jobs/{fine_tuning_job_id}/resume` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/images/edits` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/images/generations` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/images/variations` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/models` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/models/{model}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/moderations` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/admin_api_keys` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/admin_api_keys/{key_id}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/audit_logs` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/certificates` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/certificates/activate` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/certificates/deactivate` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/certificates/{certificate_id}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/costs` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/data_retention` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/groups` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/groups/{group_id}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/groups/{group_id}/roles` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/groups/{group_id}/roles/{role_id}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/groups/{group_id}/users` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/groups/{group_id}/users/{user_id}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/invites` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/invites/{invite_id}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/projects` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/projects/{project_id}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/projects/{project_id}/api_keys` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/projects/{project_id}/api_keys/{api_key_id}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/projects/{project_id}/archive` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/projects/{project_id}/certificates` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/projects/{project_id}/certificates/activate` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/projects/{project_id}/certificates/deactivate` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/projects/{project_id}/data_retention` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/projects/{project_id}/groups` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/projects/{project_id}/groups/{group_id}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/projects/{project_id}/hosted_tool_permissions` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/projects/{project_id}/model_permissions` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/projects/{project_id}/rate_limits` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/projects/{project_id}/rate_limits/{rate_limit_id}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/projects/{project_id}/service_accounts` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/projects/{project_id}/service_accounts/{service_account_id}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/projects/{project_id}/spend_alerts` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/projects/{project_id}/spend_alerts/{alert_id}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/projects/{project_id}/users` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/projects/{project_id}/users/{user_id}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/roles` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/roles/{role_id}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/spend_alerts` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/spend_alerts/{alert_id}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/usage/audio_speeches` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/usage/audio_transcriptions` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/usage/code_interpreter_sessions` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/usage/completions` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/usage/embeddings` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/usage/file_search_calls` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/usage/images` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/usage/moderations` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/usage/vector_stores` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/usage/web_search_calls` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/users` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/users/{user_id}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/users/{user_id}/roles` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/organization/users/{user_id}/roles/{role_id}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/projects/{project_id}/groups/{group_id}/roles` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/projects/{project_id}/groups/{group_id}/roles/{role_id}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/projects/{project_id}/roles` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/projects/{project_id}/roles/{role_id}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/projects/{project_id}/users/{user_id}/roles` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/projects/{project_id}/users/{user_id}/roles/{role_id}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/realtime/calls` | `direct_official_oauth_verified` | `allow` | `direct_official_oauth_or_local_proxy` | `` | Use the local proxy or the tested direct official OAuth path. Re-run the OAuth matrix for a different account or date. |
| `/v1/realtime/calls/{call_id}/accept` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/realtime/calls/{call_id}/hangup` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/realtime/calls/{call_id}/refer` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/realtime/calls/{call_id}/reject` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/realtime/client_secrets` | `direct_official_oauth_verified` | `allow` | `direct_official_oauth_or_local_proxy` | `` | Use the local proxy or the tested direct official OAuth path. Re-run the OAuth matrix for a different account or date. |
| `/v1/realtime/sessions` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/realtime/transcription_sessions` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/realtime/translations/client_secrets` | `direct_official_oauth_verified` | `allow` | `direct_official_oauth_or_local_proxy` | `` | Use the local proxy or the tested direct official OAuth path. Re-run the OAuth matrix for a different account or date. |
| `/v1/responses` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/responses/compact` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/responses/input_tokens` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/responses/{response_id}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/responses/{response_id}/cancel` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/responses/{response_id}/input_items` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/skills` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/skills/{skill_id}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/skills/{skill_id}/content` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/skills/{skill_id}/versions` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/skills/{skill_id}/versions/{version}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/skills/{skill_id}/versions/{version}/content` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/threads` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/threads/runs` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/threads/{thread_id}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/threads/{thread_id}/messages` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/threads/{thread_id}/messages/{message_id}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/threads/{thread_id}/runs` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/threads/{thread_id}/runs/{run_id}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/threads/{thread_id}/runs/{run_id}/cancel` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/threads/{thread_id}/runs/{run_id}/steps` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/threads/{thread_id}/runs/{run_id}/steps/{step_id}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/threads/{thread_id}/runs/{run_id}/submit_tool_outputs` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/uploads` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/uploads/{upload_id}/cancel` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/uploads/{upload_id}/complete` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/uploads/{upload_id}/parts` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/vector_stores` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/vector_stores/{vector_store_id}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/vector_stores/{vector_store_id}/file_batches` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/vector_stores/{vector_store_id}/file_batches/{batch_id}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/vector_stores/{vector_store_id}/file_batches/{batch_id}/cancel` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/vector_stores/{vector_store_id}/file_batches/{batch_id}/files` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/vector_stores/{vector_store_id}/files` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/vector_stores/{vector_store_id}/files/{file_id}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/vector_stores/{vector_store_id}/files/{file_id}/content` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/vector_stores/{vector_store_id}/search` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/videos` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/videos/characters` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/videos/characters/{character_id}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/videos/edits` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/videos/extensions` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/videos/{video_id}` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/videos/{video_id}/content` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
| `/v1/videos/{video_id}/remix` | `local_compat_or_chatgpt_backend_bridge` | `allow` | `local_proxy_compatibility` | `` | Use the local proxy. This is compatibility, not proof that the hosted OpenAI Platform route accepts ChatGPT/Codex OAuth. |
